import React from 'react';
import './styles/index.css';

const Brand = ({ className }) => (
	<h1 className={`b black di font-major-mono f1 ma0 purple--first-letter ${className}`}>Kanoon.</h1>
);

export default Brand;